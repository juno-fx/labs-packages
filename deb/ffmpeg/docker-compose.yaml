version: "3.5"

services:
    ffmpeg:
        build:
          context: .
          dockerfile: Dockerfile
          args:
            VERSION: ${VERSION}
        restart: "no"
        volumes:
        - ./:/output/